id: https://example.com/enum-encoding-test
name: enum-encoding-test
prefixes:
  ex: https://example.com/enum-encoding-test/
  sstatus: https://example.com/status/
default_prefix: ex
enums:
  StatusEnum:
    permissible_values:
      active:
        meaning: sstatus:Active
      not known: {}
classes:
  Item:
    attributes:
      id:
        identifier: true
      status:
        range: StatusEnum
