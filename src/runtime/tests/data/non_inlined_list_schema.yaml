id: https://example.org/noninline
name: noninline
prefixes:
  ex: https://example.org/
default_prefix: ex
default_range: string
slots:
  id:
    identifier: true
  ports:
    range: Port
    multivalued: true
    inlined: false
classes:
  Port:
    slots:
      - id
  Trail:
    slots:
      - id
      - ports
